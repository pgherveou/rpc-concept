// Root-level build file for the RPC Bridge Android Demo.
// Applies common plugin versions but defers configuration to the app module.

plugins {
    id("com.android.application") version "8.2.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.22" apply false
}
