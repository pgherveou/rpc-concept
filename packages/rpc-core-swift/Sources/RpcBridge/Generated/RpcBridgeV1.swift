// DO NOT EDIT.
// Generated by rpc-bridge-codegen from package "rpc.bridge.v1".
// Any manual changes will be overwritten on the next generation run.

import Foundation


public struct OpenBody: Codable, Sendable {
    public var method: String = ""

    public init() {}

    enum CodingKeys: String, CodingKey {
        case method
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.method = (try? container.decode(String.self, forKey: .method)) ?? ""
    }

    public init(jsonUTF8Data data: Data) throws {
        self = try JSONDecoder().decode(Self.self, from: data)
    }

    public func jsonUTF8Data() throws -> Data {
        return try JSONEncoder().encode(self)
    }
}

public struct MessageBody: Codable, Sendable {
    public var payload: AnyCodable? = nil

    public init() {}

    enum CodingKeys: String, CodingKey {
        case payload
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.payload = try? container.decode(AnyCodable.self, forKey: .payload)
    }

    public init(jsonUTF8Data data: Data) throws {
        self = try JSONDecoder().decode(Self.self, from: data)
    }

    public func jsonUTF8Data() throws -> Data {
        return try JSONEncoder().encode(self)
    }
}

public struct HalfCloseBody: Codable, Sendable {

    public init() {}

    public init(jsonUTF8Data data: Data) throws {
        self = try JSONDecoder().decode(Self.self, from: data)
    }

    public func jsonUTF8Data() throws -> Data {
        return try JSONEncoder().encode(self)
    }
}

public struct CloseBody: Codable, Sendable {

    public init() {}

    public init(jsonUTF8Data data: Data) throws {
        self = try JSONDecoder().decode(Self.self, from: data)
    }

    public func jsonUTF8Data() throws -> Data {
        return try JSONEncoder().encode(self)
    }
}

public struct CancelBody: Codable, Sendable {

    public init() {}

    public init(jsonUTF8Data data: Data) throws {
        self = try JSONDecoder().decode(Self.self, from: data)
    }

    public func jsonUTF8Data() throws -> Data {
        return try JSONEncoder().encode(self)
    }
}

public struct ErrorBody: Codable, Sendable {
    public var errorCode: UInt32 = 0
    public var errorMessage: String = ""
    public var details: AnyCodable? = nil

    public init() {}

    enum CodingKeys: String, CodingKey {
        case errorCode
        case errorMessage
        case details
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.errorCode = (try? container.decode(UInt32.self, forKey: .errorCode)) ?? 0
        self.errorMessage = (try? container.decode(String.self, forKey: .errorMessage)) ?? ""
        self.details = try? container.decode(AnyCodable.self, forKey: .details)
    }

    public init(jsonUTF8Data data: Data) throws {
        self = try JSONDecoder().decode(Self.self, from: data)
    }

    public func jsonUTF8Data() throws -> Data {
        return try JSONEncoder().encode(self)
    }
}

public enum RpcFrameBody: Sendable {
    case open(OpenBody)
    case message(MessageBody)
    case halfClose
    case close
    case cancel
    case error(ErrorBody)
    case unknown
}

public struct RpcFrame: Codable, Sendable {
    public var streamId: UInt32 = 0
    public var body: RpcFrameBody = .unknown

    public init(streamId: UInt32 = 0, body: RpcFrameBody = .unknown) {
        self.streamId = streamId
        self.body = body
    }

    enum CodingKeys: String, CodingKey {
        case streamId
        case open
        case message
        case halfClose
        case close
        case cancel
        case error
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.streamId = (try? container.decode(UInt32.self, forKey: .streamId)) ?? 0
        if let v = try? container.decode(OpenBody.self, forKey: .open) {
            self.body = .open(v)
        } else if let v = try? container.decode(MessageBody.self, forKey: .message) {
            self.body = .message(v)
        } else if container.contains(.halfClose) {
            self.body = .halfClose
        } else if container.contains(.close) {
            self.body = .close
        } else if container.contains(.cancel) {
            self.body = .cancel
        } else if let v = try? container.decode(ErrorBody.self, forKey: .error) {
            self.body = .error(v)
        } else {
            self.body = .unknown
        }
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(streamId, forKey: .streamId)
        switch body {
        case .open(let v):
            try container.encode(v, forKey: .open)
        case .message(let v):
            try container.encode(v, forKey: .message)
        case .halfClose:
            try container.encode(HalfCloseBody(), forKey: .halfClose)
        case .close:
            try container.encode(CloseBody(), forKey: .close)
        case .cancel:
            try container.encode(CancelBody(), forKey: .cancel)
        case .error(let v):
            try container.encode(v, forKey: .error)
        case .unknown: break
        }
    }

    public init(jsonUTF8Data data: Data) throws {
        self = try JSONDecoder().decode(Self.self, from: data)
    }

    public func jsonUTF8Data() throws -> Data {
        return try JSONEncoder().encode(self)
    }
}
