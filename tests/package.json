{
  "name": "@rpc-bridge/tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "node --test --experimental-vm-modules dist/src/*.test.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@rpc-bridge/core": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
