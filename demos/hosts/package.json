{
  "name": "@rpc-bridge/hosts",
  "version": "0.1.0",
  "private": true,
  "description": "TruAPI host implementations: web, iOS, Android, Electron with mock services",
  "type": "module",
  "scripts": {
    "build": "tsc && node build.js",
    "serve": "npx serve dist"
  },
  "dependencies": {
    "@rpc-bridge/core": "0.1.0",
    "@rpc-bridge/transport-web": "0.1.0",
    "@rpc-bridge/transport-ios": "0.1.0",
    "@rpc-bridge/transport-android": "0.1.0",
    "@rpc-bridge/transport-electron": "0.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "typescript": "^5.4.0"
  }
}
