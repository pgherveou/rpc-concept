// -----------------------------------------------------------------
// Auto-generated by @rpc-bridge/codegen -- DO NOT EDIT
// -----------------------------------------------------------------

import { ProtoWriter, ProtoReader } from '@rpc-bridge/core';

/** Message: HelloRequest */
export interface IHelloRequest {
  name: string;
  language: string;
}

export class HelloRequest implements IHelloRequest {
  name: string = '';
  language: string = '';

  constructor(init?: Partial<IHelloRequest>) {
    if (init) {
      Object.assign(this, init);
    }
  }

  /** Encode this message to protobuf binary format. */
  static encode(msg: IHelloRequest): Uint8Array {
    const w = new ProtoWriter();
    w.writeStringField(1, msg.name);
    w.writeStringField(2, msg.language);
    return w.finish();
  }

  /** Decode a HelloRequest from protobuf binary format. */
  static decode(data: Uint8Array): HelloRequest {
    const r = new ProtoReader(data);
    const msg = new HelloRequest();
    while (r.hasMore()) {
      const tag = r.readTag();
      const fieldNumber = tag >>> 3;
      const wireType = tag & 0x7;
      switch (fieldNumber) {
        case 1: {
          msg.name = r.readString();
          break;
        }
        case 2: {
          msg.language = r.readString();
          break;
        }
        default:
          r.skipField(wireType);
          break;
      }
    }
    return msg;
  }
}

/** Message: HelloResponse */
export interface IHelloResponse {
  message: string;
  timestamp: number;
  serverVersion: string;
}

export class HelloResponse implements IHelloResponse {
  message: string = '';
  timestamp: number = 0;
  serverVersion: string = '';

  constructor(init?: Partial<IHelloResponse>) {
    if (init) {
      Object.assign(this, init);
    }
  }

  /** Encode this message to protobuf binary format. */
  static encode(msg: IHelloResponse): Uint8Array {
    const w = new ProtoWriter();
    w.writeStringField(1, msg.message);
    w.writeVarintField(2, msg.timestamp);
    w.writeStringField(3, msg.serverVersion);
    return w.finish();
  }

  /** Decode a HelloResponse from protobuf binary format. */
  static decode(data: Uint8Array): HelloResponse {
    const r = new ProtoReader(data);
    const msg = new HelloResponse();
    while (r.hasMore()) {
      const tag = r.readTag();
      const fieldNumber = tag >>> 3;
      const wireType = tag & 0x7;
      switch (fieldNumber) {
        case 1: {
          msg.message = r.readString();
          break;
        }
        case 2: {
          msg.timestamp = r.readVarint();
          break;
        }
        case 3: {
          msg.serverVersion = r.readString();
          break;
        }
        default:
          r.skipField(wireType);
          break;
      }
    }
    return msg;
  }
}

/** Message: GreetingStreamRequest */
export interface IGreetingStreamRequest {
  name: string;
  maxCount: number;
  intervalMs: number;
}

export class GreetingStreamRequest implements IGreetingStreamRequest {
  name: string = '';
  maxCount: number = 0;
  intervalMs: number = 0;

  constructor(init?: Partial<IGreetingStreamRequest>) {
    if (init) {
      Object.assign(this, init);
    }
  }

  /** Encode this message to protobuf binary format. */
  static encode(msg: IGreetingStreamRequest): Uint8Array {
    const w = new ProtoWriter();
    w.writeStringField(1, msg.name);
    w.writeVarintField(2, msg.maxCount);
    w.writeVarintField(3, msg.intervalMs);
    return w.finish();
  }

  /** Decode a GreetingStreamRequest from protobuf binary format. */
  static decode(data: Uint8Array): GreetingStreamRequest {
    const r = new ProtoReader(data);
    const msg = new GreetingStreamRequest();
    while (r.hasMore()) {
      const tag = r.readTag();
      const fieldNumber = tag >>> 3;
      const wireType = tag & 0x7;
      switch (fieldNumber) {
        case 1: {
          msg.name = r.readString();
          break;
        }
        case 2: {
          msg.maxCount = r.readVarint();
          break;
        }
        case 3: {
          msg.intervalMs = r.readVarint();
          break;
        }
        default:
          r.skipField(wireType);
          break;
      }
    }
    return msg;
  }
}

/** Message: GreetingEvent */
export interface IGreetingEvent {
  message: string;
  seq: number;
  timestamp: number;
}

export class GreetingEvent implements IGreetingEvent {
  message: string = '';
  seq: number = 0;
  timestamp: number = 0;

  constructor(init?: Partial<IGreetingEvent>) {
    if (init) {
      Object.assign(this, init);
    }
  }

  /** Encode this message to protobuf binary format. */
  static encode(msg: IGreetingEvent): Uint8Array {
    const w = new ProtoWriter();
    w.writeStringField(1, msg.message);
    w.writeVarintField(2, msg.seq);
    w.writeVarintField(3, msg.timestamp);
    return w.finish();
  }

  /** Decode a GreetingEvent from protobuf binary format. */
  static decode(data: Uint8Array): GreetingEvent {
    const r = new ProtoReader(data);
    const msg = new GreetingEvent();
    while (r.hasMore()) {
      const tag = r.readTag();
      const fieldNumber = tag >>> 3;
      const wireType = tag & 0x7;
      switch (fieldNumber) {
        case 1: {
          msg.message = r.readString();
          break;
        }
        case 2: {
          msg.seq = r.readVarint();
          break;
        }
        case 3: {
          msg.timestamp = r.readVarint();
          break;
        }
        default:
          r.skipField(wireType);
          break;
      }
    }
    return msg;
  }
}

/** Message: CollectNamesRequest */
export interface ICollectNamesRequest {
  name: string;
}

export class CollectNamesRequest implements ICollectNamesRequest {
  name: string = '';

  constructor(init?: Partial<ICollectNamesRequest>) {
    if (init) {
      Object.assign(this, init);
    }
  }

  /** Encode this message to protobuf binary format. */
  static encode(msg: ICollectNamesRequest): Uint8Array {
    const w = new ProtoWriter();
    w.writeStringField(1, msg.name);
    return w.finish();
  }

  /** Decode a CollectNamesRequest from protobuf binary format. */
  static decode(data: Uint8Array): CollectNamesRequest {
    const r = new ProtoReader(data);
    const msg = new CollectNamesRequest();
    while (r.hasMore()) {
      const tag = r.readTag();
      const fieldNumber = tag >>> 3;
      const wireType = tag & 0x7;
      switch (fieldNumber) {
        case 1: {
          msg.name = r.readString();
          break;
        }
        default:
          r.skipField(wireType);
          break;
      }
    }
    return msg;
  }
}

/** Message: CollectNamesResponse */
export interface ICollectNamesResponse {
  message: string;
  count: number;
}

export class CollectNamesResponse implements ICollectNamesResponse {
  message: string = '';
  count: number = 0;

  constructor(init?: Partial<ICollectNamesResponse>) {
    if (init) {
      Object.assign(this, init);
    }
  }

  /** Encode this message to protobuf binary format. */
  static encode(msg: ICollectNamesResponse): Uint8Array {
    const w = new ProtoWriter();
    w.writeStringField(1, msg.message);
    w.writeVarintField(2, msg.count);
    return w.finish();
  }

  /** Decode a CollectNamesResponse from protobuf binary format. */
  static decode(data: Uint8Array): CollectNamesResponse {
    const r = new ProtoReader(data);
    const msg = new CollectNamesResponse();
    while (r.hasMore()) {
      const tag = r.readTag();
      const fieldNumber = tag >>> 3;
      const wireType = tag & 0x7;
      switch (fieldNumber) {
        case 1: {
          msg.message = r.readString();
          break;
        }
        case 2: {
          msg.count = r.readVarint();
          break;
        }
        default:
          r.skipField(wireType);
          break;
      }
    }
    return msg;
  }
}

/** Message: ChatMessage */
export interface IChatMessage {
  from: string;
  text: string;
  seq: number;
  timestamp: number;
}

export class ChatMessage implements IChatMessage {
  from: string = '';
  text: string = '';
  seq: number = 0;
  timestamp: number = 0;

  constructor(init?: Partial<IChatMessage>) {
    if (init) {
      Object.assign(this, init);
    }
  }

  /** Encode this message to protobuf binary format. */
  static encode(msg: IChatMessage): Uint8Array {
    const w = new ProtoWriter();
    w.writeStringField(1, msg.from);
    w.writeStringField(2, msg.text);
    w.writeVarintField(3, msg.seq);
    w.writeVarintField(4, msg.timestamp);
    return w.finish();
  }

  /** Decode a ChatMessage from protobuf binary format. */
  static decode(data: Uint8Array): ChatMessage {
    const r = new ProtoReader(data);
    const msg = new ChatMessage();
    while (r.hasMore()) {
      const tag = r.readTag();
      const fieldNumber = tag >>> 3;
      const wireType = tag & 0x7;
      switch (fieldNumber) {
        case 1: {
          msg.from = r.readString();
          break;
        }
        case 2: {
          msg.text = r.readString();
          break;
        }
        case 3: {
          msg.seq = r.readVarint();
          break;
        }
        case 4: {
          msg.timestamp = r.readVarint();
          break;
        }
        default:
          r.skipField(wireType);
          break;
      }
    }
    return msg;
  }
}
